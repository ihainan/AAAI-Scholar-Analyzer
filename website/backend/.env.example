# Server Configuration
HOST=0.0.0.0
PORT=37801

# Data Directories (relative to project root or absolute paths)
DATA_DIR=../../data
CONFIG_DIR=../../config

# Cache Configuration
AVATAR_CACHE_DIR=./avatar_cache
AVATAR_FAIL_CACHE_TTL=3600  # 1 hour in seconds for failed avatar fetches
CACHE_TTL_SECONDS=300        # 5 minutes for JSON file cache

# HTTP Client Configuration
HTTP_TIMEOUT=5.0             # Timeout for avatar fetching

# CORS Configuration (comma-separated origins, use * for all)
CORS_ORIGINS=*

# Logging
LOG_LEVEL=INFO
