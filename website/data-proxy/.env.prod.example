# Production Environment Configuration for Data Proxy Service
# Copy this file to .env.prod and adjust values as needed
# Docker Compose will read this file automatically

# Environment
ENV=production

# Docker Configuration
HOST_PORT=37803              # Port exposed on host machine
CACHE_HOST_DIR=./cache_data  # Cache directory on host machine

# Server Configuration (used by application inside container)
HOST=0.0.0.0
PORT=37803                   # Port application listens on (must match docker port mapping)

# Cache Configuration (Docker path)
CACHE_DIR=/app/cache
AMINER_CACHE_TTL=1296000  # 15 days in seconds
AVATAR_CACHE_TTL=31536000  # 365 days in seconds (effectively permanent)

# Firecrawl Configuration
FIRECRAWL_API_URL=https://firecrawl.ihainan.me/v1
FIRECRAWL_TIMEOUT=180  # Firecrawl can be slow, allow 3 minutes (use integer for uv compatibility)

# HTTP Client Configuration
HTTP_TIMEOUT=30

# CORS Configuration (comma-separated origins, use * for all)
# In production, replace * with specific origins
CORS_ORIGINS=*

# Logging
LOG_LEVEL=INFO
